namespace System.Validations.Extensions;

public static class DecimalExtensions {
    //public static INumberValidations<decimal> IsNullOr(this decimal? subject, [CallerArgumentExpression(nameof(subject))] string? source = null)
    //    => NumberValidations<decimal>.Create(subject, source!);
    //public static IValidationsConnector<INumberValidations<decimal>> IsNotNull(this decimal? subject, [CallerArgumentExpression(nameof(subject))] string? source = null)
    //    => new ValidationsConnector<INumberValidations<decimal>>(new NumberValidations<decimal>(subject, source!, Validations.EnsureNotNull(subject, source!)));
}