namespace System.Validations.Extensions;

public static class IntegerExtensions {
    //public static INumberValidations<int> IsNullOr(this int? subject, [CallerArgumentExpression(nameof(subject))] string? source = null)
    //    => new NumberValidations<int>(subject, source!);
    //public static IValidationsConnector<INumberValidations<int>> IsNotNull(this int? subject, [CallerArgumentExpression(nameof(subject))] string? source = null)
    //    => new ValidationsConnector<INumberValidations<int>>(new NumberValidations<int>(subject, source!, Validations.EnsureNotNull(subject, source!)));
}